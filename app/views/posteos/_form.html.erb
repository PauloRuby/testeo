<%= form_with(model: posteo, local: true) do |form| %>
  <% if posteo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(posteo.errors.count, "error") %> prohibited this posteo from being saved:</h2>

      <ul>
      <% posteo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_field :titulo %>
  </div>

  <div class="field">
    <%= form.label :cuerpo %>
    <%= form.text_area :cuerpo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
